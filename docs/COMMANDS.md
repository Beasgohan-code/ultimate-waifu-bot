<!-- GENERATED by scripts/gen_reference_docs.py — do not edit. Run `make docs` (or the script) after adding or renaming a command. -->

# Command reference

Every command the bot actually listens for, read out of the routers at generation time.
Descriptions are the handler docstrings, so the first line of a handler's docstring is
user-facing text — write it like UI copy.

Two rules the code follows and this file reflects:

* **no command is claimed twice** — aiogram stops at the first matching handler, so a
  duplicate name is a dead command rather than an error (enforced by
  `tests/test_plugin_registry.py`);
* **aliases are free, names are not** — the reference bot's spellings are kept as aliases
  where they collide with nothing, so muscle memory from the old bot keeps working.


**144 commands across 23 plugins (31 routers registered — the extra ones render no commands: inline mode, the autoadd feed, member/reaction/poll updates).**

## `misc.py`

| command | description |
| --- | --- |
| `/about` | — |
| `/commands` | The complete command list, generated from the routers and sent as a file. |
| `/font` | /font lists the styles, /font gothic picks one (parity with the old bot). |
| `/help` | — |
| `/invite` | — |
| `/language` | /language shows what is translated; /language ja sets it. |
| `/ping` | — |
| `/settings` | — |
| `/start` | with an optional ref-<id> deep link argument (/invite). |
| `/update` | Show the shipped changelog (the reference bot pasted a hard-coded string). |

## `players.py`

| command | description |
| --- | --- |
| `/bio` | /bio <text> sets the line under your name (their bot had no bio at all). |
| `/glow` | Their /profileglow: the name banner on your card. Off by default. |
| `/lvl` | — |
| `/pcard` | the image card, not the text sheet. |
| `/privacy` | — |
| `/profile` | — |
| `/register` | it is a no-op if |
| `/setname` | Display name inside the bot (Telegram's own name is not touched). |

## `characters.py`

| command | description |
| --- | --- |
| `/addchar` | /addchar Name | Series | Rarity | price | tags (+ attach a photo or video). |
| `/banner` | Weight specific characters toward the banner (their bot had no way to promote one). |
| `/chancelist` | Both ladders at once: what the gacha pays and what a free claim pays. |
| `/char` | the character sheet, price, holders and spawn stats. |
| `/chars` | Browse the roster by tier: /chars, /chars 3, /chars gojo. |
| `/delchar` | — |
| `/hidechar` | Deactivate instead of delete: keeps history intact, stops future drops. |
| `/media` | store the bot's own file_id. |
| `/reseed` | Load the optional catalogue into an empty roster (idempotent, name+series deduped). |
| `/setchance` | /setchance Legendary 6.2 (and --claim for the free-claim table). |
| `/setclaim` | the claim ladder only (gacha rates: /setchance). |

## `uploads.py`

| command | description |
| --- | --- |
| `/archiveart` | re-host art into a permanent file_id. |
| `/autoadd` | this chat's media feed in, without a command per character. |
| `/rosterstats` | per-tier counts plus what ingestion still has open. |
| `/upload` | creates the character. |
| `/uploads` | which characters are still waiting for a host choice. |

## `gacha.py`

| command | description |
| --- | --- |
| `/chances` | Both tables (summon and free claim), because hiding one is how you get banned. |
| `/claimlist` | their bot called this /claimlist. |
| `/guarantee` | — |
| `/hclaim` | The free daily claim (their /hclaim), with the premium double-claim bonus. |
| `/pull` | — |
| `/pulls` | — |
| `/rolls` | — |
| `/verify` | — |

## `collection.py`

| command | description |
| --- | --- |
| `/check` | — |
| `/collection` | — |
| `/fav` | — |
| `/harem` | same renderer, one default. |
| `/lock` | — |
| `/mystats` | — |
| `/search` | — |
| `/sell` | by collection-page number, id, or name. |

## `economy.py`

| command | description |
| --- | --- |
| `/balance` | — |
| `/bomb` | — |
| `/daily` | — |
| `/give` | — |
| `/history` | — |
| `/integrity` | a player audits their own wallet: the invariant, on demand. |
| `/paywall` | Premium perks + the boost rebate (/boost parity with their boost rewards). |
| `/rob` | — |
| `/spin` | Daily free spin, and /spin <amount> to gamble coins (their /spin). |
| `/topup` | — |
| `/work` | — |

## `market.py`

| command | description |
| --- | --- |
| `/buy` | one copy at the listed price (their /buy). |
| `/buyitem` | — |
| `/market` | — |
| `/price` | — |

## `shop.py`

| command | description |
| --- | --- |
| `/heists` | — |
| `/shop` | — |
| `/skip` | 1 resets |
| `/use` | — |

## `auctions.py`

| command | description |
| --- | --- |
| `/auction` | list one of your copies. |
| `/auctions` | — |
| `/bid` | — |
| `/cancelauction` | — |
| `/myauctions` | — |

## `trades.py`

| command | description |
| --- | --- |
| `/taccept` | — |
| `/tcancel` | — |
| `/trade` | <ids> want:<ids> [cash:<amount>] |
| `/tradeid` | offers can be shared as a code, for cross-chat trades. |
| `/trades` | — |

## `codes.py`

| command | description |
| --- | --- |
| `/addcode` | /addcode CODE coins=5000 uses=25 hours=72 premium=48 character=42 |
| `/code` | — |
| `/codeclaims` | the answer to 'the code was shared, take it back'. |
| `/codes` | — |
| `/delcode` | — |

## `gifts.py`

| command | description |
| --- | --- |
| `/anon` | same code path, one flag. |
| `/gift` | /gift @player <character id|name> [note]  ·  /gift @player 5000 (coins) |
| `/giftlog` | — |

## `premium.py`

| command | description |
| --- | --- |
| `/boosters` | — |
| `/premium` | — |
| `/raffle` | coins to a random reactor (uses the poll router in misc). |

## `progress.py`

| command | description |
| --- | --- |
| `/achievements` | — |
| `/quests` | — |
| `/streak` | — |
| `/weekly` | — |

## `stats.py`

| command | description |
| --- | --- |
| `/server` | this chat's numbers; /server all — the whole instance. |
| `/top` | — |
| `/trend` | — |
| `/usage` | — |

## `spawns.py`

| command | description |
| --- | --- |
| `/autospan` | this group's feed settings. |
| `/claim` | /claim (first responder wins) or /claim <name> (must match the spawn). |
| `/hint` | Spend a hint (an item or coins) to narrow the current spawn. |
| `/spawn` | Show the live spawn in this chat (their bot had no way to ask "is one out?"). |
| `/spawnlog` | — |
| `/summon` | /summon <name> spawn a specific character · /summon a random high tier. |

## `nguess.py`

| command | description |
| --- | --- |
| `/ngpoll` | Same round, native poll UI (four candidate names + the answer is a real quiz). |
| `/ngskip` | otherwise anyone cancels a rival's win). |
| `/ngstats` | — |
| `/ngtop` | — |
| `/nguess` | start a round in this group. |

## `moderation.py`

| command | description |
| --- | --- |
| `/ban` | Telegram action *and* a case row. |
| `/banlist` | — |
| `/case` | — |
| `/clearwarns` | — |
| `/gate` | quiz joiners in a DM before they reach the group. |
| `/gatelink` | a single-use invite with approval requests forced on. |
| `/gban` | — |
| `/groupsettings` | per-group knobs. |
| `/mute` | — |
| `/purge` | the fix for a bad config. |
| `/ungban` | — |
| `/warn` | the ladder decides what happens at 3, 4 and 5. |
| `/warnings` | — |

## `sudo.py`

| command | description |
| --- | --- |
| `/broadcast` | /broadcast <text> → every registered group, throttled by the notifier. |
| `/doctor` | Read-only health page: ledger integrity, queue depth, feature gates. |
| `/givecoins` | — |
| `/maint` | park the bot with a reason; handlers refuse while it is on. |
| `/setrole` | owner only (a role ladder that an admin |
| `/sudolist` | the config layer plus the database layer. |
| `/takecoins` | — |
| `/unpremium` | Take premium back (refund claw-backs; the ledger entry is the audit trail). |
| `/vacuum` | Housekeeping the old bot never had: activity pruning + expired-record sweeps. |

## `ai.py`

| command | description |
| --- | --- |
| `/aforget` | — |
| `/ai` | talk to your featured character (or /ai @name <text>). |
| `/aihistory` | GDPR-shaped: the player can read and delete their own transcripts. |
| `/aiusage` | — |
| `/charai` | Set/reset the persona for a character (admins; the roster's persona column). |

## `hstats.py`

| command | description |
| --- | --- |
| `/hcompletion` | — |
| `/hnote` | A pinned one-liner under /hstats (group owners asked for "rules here"). |
| `/hrarity` | — |
| `/hstats` | — |
| `/htop` | — |

## `webapp.py`

| command | description |
| --- | --- |
| `/compact` | Toggle the plain-text card mode (a11y + old clients; stored per player). |
| `/share` | Prepare an inline card and hand the player a chat picker (Bot API 9.6). |
| `/webapp` | — |

